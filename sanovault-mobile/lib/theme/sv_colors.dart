import 'package:flutter/cupertino.dart';

abstract final class SvColors {
  static const ink = Color(0xFF313647);
  static const slate = Color(0xFF435663);
  static const sage = Color(0xFFA3B087);
  static const cream = Color(0xFFFFF8D4);
  static const silver = Color(0xFFC7CEC1);
  // Compatibility names for feature-level references during migration.
  static const coral = ink;
  static const coralStrong = slate;
  static const primaryButton = ink;
  static const groupedBackground = cream;
  static const surface = Color(0xFFFFFFFF);
  static const separator = silver;
  static const danger = Color(0xFFD70015);
  static const inviteFill = Color(0xFFE8EBCF);
  static const inviteInk = ink;
}
